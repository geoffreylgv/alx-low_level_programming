#include <stdio.h>

/**
 * main - print my name (function's name)
 * @argc: first arg count
 * @argv: second arg vector
 * Return: succes compilation ok
 */

int main(int argc, char *argv[])
{
	argc = argc;

	printf("%s\n", *argv);
	return (0);
}

