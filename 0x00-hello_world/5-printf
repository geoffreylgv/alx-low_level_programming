# include <stdio.h>

/**
 * main - main block that uses printf method to print a string message
 * Return: 0 (run w/ success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
